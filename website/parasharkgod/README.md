# parasharkgod.com site source

This directory is the editable source for the public `parasharkgod.com` landing page.

Rules:

- Keep this copy in sync with the private deploy mirror repo `williamsharkey/parasharkgod`.
- `paratui` should stay front and center on the live site.
- The older Parascene Forge material should remain available as a separate section of the page, not as the default landing experience.
